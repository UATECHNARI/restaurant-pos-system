# 📌 Інструкція: Додати колонку icon в базу даних

## 🎯 Що робить цей скрипт:

Додає колонку `icon` в таблицю `products` з можливістю вибору значка:
- 🍕 Піца
- 🥗 Салат  
- 🥤 Кола
- 🍺 Алкоголь
- ☕ Кава

---

## 📋 Крок 1: Оновити базу даних

### Варіант А: Через MySQL Workbench або інший клієнт

1. Відкрити MySQL Workbench (або інший клієнт MySQL)
2. Підключитися до бази даних `bar_kitchen_pos`
3. Відкрити файл `D:\Work\Pizza\backend\add-icon-column.sql`
4. Виконати SQL скрипт

### Варіант Б: Через командний рядок MySQL

```bash
# Перейти в директорію backend
cd D:\Work\Pizza\backend

# Підключитися до MySQL (замініть root на вашого користувача)
mysql -u root -p

# Виконати скрипт
source add-icon-column.sql;

# Або скопіювати SQL команди з файлу та виконати їх
```

### Варіант В: Через PowerShell (якщо mysql в PATH)

```powershell
cd D:\Work\Pizza\backend
mysql -u root -p < add-icon-column.sql
```

---

## 📋 Крок 2: Перевірити оновлення

Після виконання SQL скрипта перевірте:

```sql
-- Перевірити структуру таблиці
DESCRIBE products;

-- Перевірити чи є колонка icon
SHOW COLUMNS FROM products LIKE 'icon';

-- Перевірити існуючі товари з їх значками
SELECT id, name, category, icon FROM products LIMIT 10;
```

---

## ✅ Що вже оновлено:

### Backend:
- ✅ `productsController.js` - обробка поля `icon` при створенні/оновленні
- ✅ SQL скрипт для додавання колонки

### Frontend:
- ✅ Тип `Product` в `App.tsx` та `api.ts` - додано поле `icon`
- ✅ `AdminPanel.tsx` - додано вибір значка в формі
- ✅ Відображення значка в таблиці товарів
- ✅ Конвертація даних з API

---

## 🚀 Після оновлення БД:

1. **Перезапустити backend** (якщо запущений):
   ```powershell
   cd D:\Work\Pizza\backend
   npm run dev
   ```

2. **Перезапустити frontend** (якщо запущений):
   ```powershell
   cd D:\Work\Pizza
   npm run dev
   ```

3. **Перевірити в адмін-панелі:**
   - Відкрити http://localhost:5173
   - Увійти як `admin` / `admin123`
   - Перейти в "Адмін-панель" → "Товари"
   - Додати або редагувати товар
   - Перевірити наявність поля "Значок" з вибором 🍕 🥗 🥤 🍺 ☕

---

## 📝 SQL Скрипт (add-icon-column.sql):

```sql
-- Додати колонку icon в таблицю products
USE bar_kitchen_pos;

ALTER TABLE products 
ADD COLUMN icon ENUM('pizza', 'salad', 'cola', 'alcohol', 'coffee') NULL DEFAULT NULL
AFTER category;

-- Оновити існуючі товари
UPDATE products 
SET icon = 'pizza' 
WHERE category = 'kitchen' AND icon IS NULL;

UPDATE products 
SET icon = 'alcohol' 
WHERE category = 'bar' AND icon IS NULL;
```

---

## 🎉 Готово!

Тепер при додаванні або редагуванні товару в адмін-панелі можна вибрати значок:
- 🍕 Піца
- 🥗 Салат
- 🥤 Кола
- 🍺 Алкоголь
- ☕ Кава

Значок відображатиметься в таблиці товарів та може використовуватись в інтерфейсі для швидкої ідентифікації товару.


