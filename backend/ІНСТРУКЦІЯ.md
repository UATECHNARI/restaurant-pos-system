# 🍕 Backend для Pizza POS System - Готовий до використання!

## ✅ Що було зроблено

### 🎯 Повний backend для системи управління рестораном

Backend успішно створено з наступним функціоналом:

#### 1. **Структура проекту**
- ✅ Правильна організація файлів та папок
- ✅ ES Modules (сучасний стандарт Node.js)
- ✅ Чиста архітектура з розділенням відповідальності

#### 2. **Автентифікація та безпека**
- ✅ JWT токени для автентифікації
- ✅ Bcrypt хешування паролів
- ✅ 4 ролі: Admin, Касир, Кухня, Бар
- ✅ Захист endpoints за ролями
- ✅ Helmet для безпеки HTTP headers
- ✅ CORS конфігурація

#### 3. **База даних MySQL**
- ✅ Повна SQL схема з індексами
- ✅ 5 таблиць: users, products, tables, orders, order_items
- ✅ Foreign keys та constraints
- ✅ Views для статистики
- ✅ Connection pooling для продуктивності

#### 4. **REST API Endpoints**

##### 🔐 Автентифікація (`/api/auth`)
- POST /register - реєстрація
- POST /login - вхід
- GET /profile - профіль користувача

##### 🍕 Продукти (`/api/products`)
- GET / - список продуктів (з фільтрами)
- GET /:id - продукт за ID
- POST / - створити (admin)
- PUT /:id - оновити (admin)
- DELETE /:id - видалити (admin)
- PATCH /:id/toggle - перемкнути доступність

##### 🪑 Столи (`/api/tables`)
- GET / - список столів
- GET /:number - стіл за номером
- POST / - створити стіл (admin)
- PUT /:number/status - оновити статус
- DELETE /:number - видалити (admin)

##### 📋 Замовлення (`/api/orders`)
- GET / - всі замовлення (з фільтрами)
- GET /:id - замовлення за ID
- POST / - створити замовлення
- PUT /:id/status - оновити статус

##### 📊 Статистика (`/api/statistics`) - тільки Admin
- GET /dashboard - загальна статистика
- GET /sales - статистика продажів
- GET /orders-timeline - хронологія замовлень
- GET /tables - статистика столів
- GET /staff - статистика працівників

#### 5. **WebSocket (Socket.IO)**
- ✅ Real-time оновлення замовлень
- ✅ Real-time оновлення столів
- ✅ Сповіщення кухні та бару
- ✅ Room підтримка

#### 6. **Додаткові функції**
- ✅ Error handling middleware
- ✅ Input валідація
- ✅ Sanitization utils
- ✅ Логування
- ✅ Health check endpoint
- ✅ Graceful shutdown

---

## 📁 Створені файли

```
backend/
├── src/
│   ├── config/
│   │   └── database.js           ← MySQL конфігурація
│   ├── controllers/
│   │   ├── authController.js     ← Контролер авторизації
│   │   ├── ordersController.js   ← Контролер замовлень
│   │   ├── productsController.js ← Контролер продуктів
│   │   ├── tableController.js    ← Контролер столів
│   │   └── statsController.js    ← Контролер статистики
│   ├── middleware/
│   │   ├── auth.js               ← JWT middleware
│   │   └── roleCheck.js          ← Перевірка ролей
│   ├── routes/
│   │   ├── auth.js
│   │   ├── orders.js
│   │   ├── products.js
│   │   ├── tables.js
│   │   └── statistics.js
│   ├── utils/
│   │   ├── jwt.js                ← JWT утиліти
│   │   └── validation.js         ← Валідація
│   └── server.js                 ← Головний файл
├── database-schema.sql           ← SQL схема БД
├── test-connection.js            ← Тест з'єднання
├── init-users.js                 ← Ініціалізація користувачів
├── .env.example                  ← Шаблон налаштувань
├── .gitignore
├── package.json
├── README.md                     ← Повна англійська документація
├── QUICKSTART.md                 ← Швидкий старт (англ)
├── SETUP_SUMMARY.md              ← Підсумок (англ)
└── ІНСТРУКЦІЯ.md                 ← Ця інструкція
```

---

## 🚀 Як запустити (Покрокова інструкція)

### Крок 1: Встановити залежності

```bash
cd backend
npm install
```

### Крок 2: Налаштувати MySQL

#### 2.1 Створити базу даних

Відкрийте MySQL консоль:

```bash
mysql -u root -p
```

Виконайте команди:

```sql
CREATE DATABASE bar_kitchen_pos CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

#### 2.2 Імпортувати схему

```bash
mysql -u root -p bar_kitchen_pos < database-schema.sql
```

### Крок 3: Створити файл .env

**На Windows (PowerShell):**
```powershell
copy .env.example .env
notepad .env
```

**На Linux/Mac:**
```bash
cp .env.example .env
nano .env
```

**Відредагуйте .env файл:**

```env
PORT=3001
NODE_ENV=development

# ⚠️ Обов'язково змініть DB_PASSWORD на ваш пароль MySQL!
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=ваш_пароль_mysql
DB_NAME=bar_kitchen_pos
DB_PORT=3306

# ⚠️ Для production використовуйте складний секретний ключ!
JWT_SECRET=ваш_секретний_ключ_мінімум_32_символи
JWT_EXPIRES_IN=24h

FRONTEND_URL=http://localhost:5173
```

### Крок 4: Перевірити з'єднання з базою даних

```bash
npm run test:db
```

Якщо все правильно, побачите:
```
✅ MySQL connection successful!
✅ Test query successful
📋 Available tables:
  - users
  - products
  - tables
  - orders
  - order_items
✅ All checks passed!
```

### Крок 5: Створити тестових користувачів

```bash
npm run init:users
```

Це створить 4 користувачів з паролем `password123`:
- `admin@pizza.com` - адміністратор
- `cashier@pizza.com` - касир
- `kitchen@pizza.com` - кухня
- `bar@pizza.com` - бар

### Крок 6: Запустити сервер

**Для розробки (з автоматичним перезавантаженням):**
```bash
npm run dev
```

**Для production:**
```bash
npm start
```

Якщо все працює, побачите:
```
═════════════════════════════════════════
🚀 Server running on port 3001
📡 WebSocket ready
🌍 Environment: development
🔗 API: http://localhost:3001
🏥 Health: http://localhost:3001/health
═════════════════════════════════════════
```

### Крок 7: Перевірити роботу API

**Відкрийте новий термінал і виконайте:**

#### Health Check
```bash
curl http://localhost:3001/health
```

#### Тест логіну
```bash
curl -X POST http://localhost:3001/api/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"admin@pizza.com\",\"password\":\"password123\"}"
```

Отримаєте токен у відповіді!

---

## 📱 Тестування з Postman

### 1. Завантажте Postman
https://www.postman.com/downloads/

### 2. Імпортуйте Collection

Створіть нову колекцію в Postman з такими запитами:

#### POST Login
```
URL: http://localhost:3001/api/auth/login
Method: POST
Body (JSON):
{
  "email": "admin@pizza.com",
  "password": "password123"
}
```

#### GET Products (з токеном)
```
URL: http://localhost:3001/api/products
Method: GET
Headers:
  Authorization: Bearer YOUR_TOKEN_HERE
```

---

## 🎓 Використання API

### Приклад повного процесу:

#### 1. Логін
```javascript
POST /api/auth/login
{
  "email": "cashier@pizza.com",
  "password": "password123"
}
→ Отримуєте токен
```

#### 2. Отримати продукти
```javascript
GET /api/products
Headers: Authorization: Bearer TOKEN
→ Список всіх продуктів
```

#### 3. Створити замовлення
```javascript
POST /api/orders
Headers: Authorization: Bearer TOKEN
{
  "table_number": 5,
  "comment": "Без цибулі",
  "items": [
    { "product_id": 1, "quantity": 2 },
    { "product_id": 7, "quantity": 1 }
  ]
}
→ Створюється замовлення + WebSocket оповіщення
```

#### 4. Оновити статус (Кухня/Бар)
```javascript
PUT /api/orders/1/status
Headers: Authorization: Bearer TOKEN
{
  "status": "preparing"
}
→ Статус оновлюється + WebSocket оповіщення
```

---

## 🔧 Корисні команди

```bash
# Встановлення
npm install

# Тест БД
npm run test:db

# Створити користувачів
npm run init:users

# Запуск розробка
npm run dev

# Запуск production
npm start
```

---

## 🐛 Вирішення проблем

### ❌ Помилка "MySQL connection error"

**Рішення:**
1. Перевірте, що MySQL запущений
2. Перевірте пароль у .env файлі
3. Перевірте, що база даних створена:
   ```bash
   mysql -u root -p -e "SHOW DATABASES;"
   ```

### ❌ Помилка "Port already in use"

**Рішення (Windows):**
```bash
netstat -ano | findstr :3001
taskkill /PID <номер_процесу> /F
```

### ❌ Помилка "Cannot find module"

**Рішення:**
```bash
del /s /q node_modules
npm install
```

---

## 📚 Документація

- **Англійська повна документація:** [README.md](README.md)
- **Швидкий старт:** [QUICKSTART.md](QUICKSTART.md)
- **Підсумок налаштування:** [SETUP_SUMMARY.md](SETUP_SUMMARY.md)

---

## 🔒 Безпека для Production

⚠️ **ОБОВ'ЯЗКОВО для продакшн:**

1. Змініть JWT_SECRET на випадковий ключ (32+ символи)
2. Змініть всі паролі користувачів
3. Налаштуйте HTTPS
4. Налаштуйте firewall
5. Використовуйте .env файл (не комітьте його в git!)
6. Регулярно оновлюйте залежності

---

## 🎉 Готово!

Ваш backend працює на `http://localhost:3001`

### Тестові облікові записи:

| Email | Пароль | Роль |
|-------|--------|------|
| admin@pizza.com | password123 | Адміністратор |
| cashier@pizza.com | password123 | Касир |
| kitchen@pizza.com | password123 | Кухня |
| bar@pizza.com | password123 | Бар |

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте [QUICKSTART.md](QUICKSTART.md) розділ "Типові проблеми"
2. Перевірте логи в консолі
3. Виконайте `npm run test:db` для діагностики

---

**Успішного використання! 🚀**

*Створено: ${new Date().toLocaleDateString('uk-UA')}*




