# 🛒 ЯК ДОДАТИ ТОВАРИ В БАЗУ ДАНИХ

## 📦 Що буде додано:

### 🍕 Піца (6 позицій):
- Маргарита - 150 грн
- Пепероні - 180 грн
- Чотири сири - 200 грн
- Гавайська - 170 грн
- М'ясна - 220 грн
- Вегетаріанська - 160 грн

### ☕ Кава (5 позицій):
- Еспресо - 45 грн
- Американо - 50 грн
- Капучино - 60 грн
- Лате - 65 грн
- Флет Вайт - 70 грн

### 🥤 Напої (6 позицій):
- Кока-Кола - 35 грн
- Кока-Кола Зеро - 35 грн
- Фанта - 35 грн
- Спрайт - 35 грн
- Вода мінеральна - 25 грн
- Сік апельсиновий - 40 грн

---

## 🚀 СПОСІБ 1: Автоматично (найпростіше)

Відкрийте PowerShell в папці `backend` і виконайте:

```powershell
cd D:\Work\Pizza\backend
npm run add:products
```

Готово! Всі товари будуть додані автоматично.

---

## 🔧 СПОСІБ 2: Через MySQL

Якщо хочете додати товари вручну через MySQL:

```powershell
# 1. Відкрийте MySQL
mysql -u root -p

# 2. Виконайте SQL файл
source D:/Work/Pizza/backend/add-products.sql
```

Або скопіюйте вміст файлу `add-products.sql` і виконайте в MySQL Workbench.

---

## ✅ ПЕРЕВІРКА

Після додавання товарів перевірте їх в базі:

```sql
-- Показати всі товари
SELECT * FROM products ORDER BY category, name;

-- Підрахувати товари по категоріях
SELECT category, COUNT(*) as count FROM products GROUP BY category;
```

---

## 📝 РЕДАГУВАННЯ ТОВАРІВ

Якщо потрібно змінити ціну або назву:

```sql
-- Змінити ціну
UPDATE products SET price = 160.00 WHERE name = 'Маргарита';

-- Змінити опис
UPDATE products SET description = 'Нова класична піца' WHERE name = 'Маргарита';

-- Зробити товар недоступним
UPDATE products SET available = 0 WHERE name = 'Гавайська';
```

---

## ➕ ДОДАТИ СВОЇ ТОВАРИ

Відредагуйте файл `add-products.js` і додайте свої товари в масиви:

```javascript
// Додати свою піцу (категорія: kitchen)
const pizzas = [
  // ... існуючі піци
  { name: '🍕 Своя піца', price: 250.00, category: 'kitchen', description: 'Опис' }
];

// Додати свій напій (категорія: bar)
const drinks = [
  // ... існуючі напої
  { name: '🥤 Свій напій', price: 50.00, category: 'bar', description: 'Опис' }
];
```

**ВАЖЛИВО:** Категорії можуть бути тільки:
- `kitchen` - для їжі (піца, страви)
- `bar` - для напоїв (кава, кола, соки)

Потім знову запустіть `npm run add:products`.

---

## 🎯 ВАЖЛИВО!

- Товари додаються **БЕЗ видалення** існуючих
- Якщо потрібно **очистити таблицю**, виконайте спочатку:
  ```sql
  DELETE FROM products;
  ```
- Перевірте що backend **НЕ запущений** під час додавання товарів

---

## 🆘 ПРОБЛЕМИ?

**Помилка: Duplicate entry**
- Товар вже існує. Видаліть або змініть назву.

**Помилка: Cannot add or update a child row**
- Переконайтесь що таблиця `products` існує.

**Помилка: Access denied**
- Перевірте `.env` файл та налаштування MySQL.

---

Готово! Товари готові до продажу! 🎉

