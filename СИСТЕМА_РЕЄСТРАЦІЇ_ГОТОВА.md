# ✅ СИСТЕМА РЕЄСТРАЦІЇ ГОТОВА!

## 🎯 Що було зроблено:

### 📋 НОВА ЛОГІКА РЕЄСТРАЦІЇ:

1. **Клієнт (Адміністратор) реєструється сам:**
   - Вводить свій email та пароль
   - Отримує роль `admin` автоматично
   - Реєстрація доступна на екрані логіну

2. **Користувачі з ролями (Кухня, Бар, Касир):**
   - Створюються через адмін-панель
   - Логін та пароль генеруються **автоматично**
   - Адмін бачить логін та пароль тільки один раз при створенні

---

## 🔧 Що було змінено:

### Backend:

#### 1. **`authController.js`** - Оновлено реєстрацію:
```javascript
// Тепер register створює тільки користувачів з роллю 'admin'
// Роль більше не передається в запиті
```

#### 2. **`passwordGenerator.js`** - Новий файл:
- `generatePassword()` - генерація випадкового пароля (12 символів)
- `generateLogin()` - генерація логіну за форматом: `{role}{number}@pizza.com`
- `generateUniqueLogin()` - генерація унікального логіну

#### 3. **`usersController.js`** - Новий контролер:
- `getAllUsers()` - отримати всіх користувачів
- `getUserById()` - отримати користувача за ID
- `createUser()` - створити користувача з автогенерацією
- `updateUser()` - оновити користувача
- `resetUserPassword()` - скинути пароль (згенерувати новий)
- `deleteUser()` - видалити користувача

#### 4. **`routes/users.js`** - Новий роутер:
- Всі endpoints доступні тільки для адміністраторів
- `/api/users` - GET, POST
- `/api/users/:id` - GET, PUT, DELETE
- `/api/users/:id/reset-password` - POST

#### 5. **`server.js`** - Додано роутер:
```javascript
app.use('/api/users', usersRouter);
```

### Frontend:

#### 1. **`api.ts`** - Додано API для користувачів:
```typescript
export interface User {
  id: number;
  email: string;
  role: 'admin' | 'cashier' | 'kitchen' | 'bar';
  created_at?: string;
  password?: string; // Тільки при створенні або скиданні пароля
}

export const usersAPI = {
  getAll(),
  getById(),
  create(role),
  update(),
  resetPassword(),
  delete()
}
```

#### 2. **`LoginScreen.tsx`** - Додано форму реєстрації:
- Кнопка "Реєстрація (для клієнта)"
- Діалог з формою реєстрації
- Валідація паролів
- Повідомлення про успішну реєстрацію

#### 3. **`AdminPanel.tsx`** - Додано вкладку "Користувачі":
- Нова вкладка "Користувачі" в Tabs
- Таблиця з користувачами (крім admin)
- Кнопка "Створити користувача"
- Вибір ролі (Кухня, Бар, Касир)
- Діалог з логіном та паролем (показується один раз)
- Кнопки:
  - 🔄 Скинути пароль
  - 🗑️ Видалити користувача
- Копіювання пароля в буфер обміну

---

## 🎨 Як це виглядає:

### 1. **Екран логіну:**
- Форма входу
- Кнопка "Реєстрація (для клієнта)" внизу
- При натисканні відкривається діалог з формою:
  - Email
  - Пароль
  - Підтвердження пароля
  - Кнопка "Зареєструватися"

### 2. **Адмін-панель → Вкладка "Користувачі":**
- Таблиця з користувачами (крім адмінів):
  - Email
  - Роль (Кухня, Бар, Касир)
  - Дата створення
  - Дії (Скинути пароль, Видалити)
- Кнопка "Створити користувача"
- При створенні:
  - Вибір ролі (Кухня/Бар/Касир)
  - Автоматична генерація логіну та пароля
  - Діалог з результатом:
    - Логін (Email)
    - Пароль (з можливістю копіювання)
    - Роль
    - Попередження: "Збережіть ці дані! Пароль показано тільки один раз."

---

## 📝 Приклади використання:

### 1. **Реєстрація клієнта:**
```
1. Відкрити http://localhost:5173
2. Натиснути "Реєстрація (для клієнта)"
3. Ввести:
   - Email: client@example.com
   - Пароль: mypassword123
   - Підтвердити пароль: mypassword123
4. Натиснути "Зареєструватися"
5. Результат: ✅ Реєстрація успішна!
6. Увійти використовуючи email та пароль
```

### 2. **Створення користувача з роллю (Кухня/Бар/Касир):**
```
1. Увійти як адміністратор
2. Перейти в "Адмін-панель" → "Користувачі"
3. Натиснути "Створити користувача"
4. Вибрати роль (наприклад, "🍳 Кухня")
5. Натиснути "Створити"
6. Результат:
   - Логін: kitchen1@pizza.com
   - Пароль: aB3dE5fG7hI9
   - Роль: Кухня
7. Скопіювати пароль (або написати десь)
8. Закрити діалог
```

### 3. **Скидання пароля:**
```
1. Відкрити таблицю користувачів
2. Натиснути кнопку 🔄 (Скинути пароль) біля користувача
3. Підтвердити дію
4. З'явиться діалог з новим паролем
5. Скопіювати новий пароль
```

---

## 🔐 Безпека:

1. ✅ Реєстрація доступна тільки для ролі `admin` (клієнт)
2. ✅ Управління користувачами доступне тільки адміністраторам
3. ✅ Пароль показується тільки один раз при створенні/скиданні
4. ✅ Паролі хешуються через bcrypt
5. ✅ Валідація email та паролів на frontend та backend

---

## 🚀 API Endpoints:

### Реєстрація (публічний):
```
POST /api/auth/register
Body: { email: string, password: string }
Response: { success: true, id, email, role: 'admin', message }
```

### Користувачі (тільки для admin):

#### Отримати всіх:
```
GET /api/users?role=kitchen (опціонально)
Headers: Authorization: Bearer <token>
```

#### Створити користувача:
```
POST /api/users
Body: { role: 'kitchen' | 'bar' | 'cashier' }
Headers: Authorization: Bearer <token>
Response: { success: true, data: { id, email, role, password }, message }
```

#### Скинути пароль:
```
POST /api/users/:id/reset-password
Headers: Authorization: Bearer <token>
Response: { success: true, data: { id, email, role, password }, message }
```

#### Видалити користувача:
```
DELETE /api/users/:id
Headers: Authorization: Bearer <token>
```

---

## 📋 Формат логінів:

Автоматично генеруються логіни за форматом:
- `kitchen1@pizza.com` (перший користувач Кухня)
- `kitchen2@pizza.com` (другий користувач Кухня)
- `bar1@pizza.com` (перший користувач Бар)
- `cashier1@pizza.com` (перший користувач Касир)

Номер збільшується автоматично, якщо логін вже існує.

---

## 🎉 ГОТОВО!

Система реєстрації повністю функціональна:

1. ✅ Клієнти можуть реєструватися самостійно
2. ✅ Адмін може створювати користувачів з ролями через панель
3. ✅ Логін та пароль генеруються автоматично для ролей
4. ✅ Можливість скидати паролі
5. ✅ Безпечне зберігання паролів

**Перезапустіть backend та frontend для застосування змін!**


