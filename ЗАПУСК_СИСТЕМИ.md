# 🚀 Запуск Pizza POS System

## Швидкий старт

### ✅ Перевірте, що у вас встановлено:
- Node.js 18+
- MySQL 8.0+
- npm 9+

---

## 📋 Покрокова інструкція

### Крок 1: Запустити Backend

#### 1.1 Відкрийте термінал для backend

```powershell
cd D:\Work\Pizza\backend
```

#### 1.2 Переконайтеся, що .env налаштований

Відкрийте `.env` файл і перевірте налаштування:

```env
DB_USER=root
DB_PASSWORD=ваш_пароль_mysql
DB_NAME=bar_kitchen_pos
# ... інші налаштування
```

#### 1.3 Перевірте з'єднання з БД (опціонально)

```powershell
npm run test:db
```

#### 1.4 Запустіть backend сервер

```powershell
npm run dev
```

✅ **Очікуваний результат:**
```
═════════════════════════════════════════
🚀 Server running on port 3001
📡 WebSocket ready
🌍 Environment: development
═════════════════════════════════════════
✅ MySQL connected successfully
```

---

### Крок 2: Запустити Frontend

#### 2.1 Відкрийте НОВИЙ термінал для frontend

⚠️ **ВАЖЛИВО:** Не закривайте термінал backend!

```powershell
cd D:\Work\Pizza
```

#### 2.2 Запустіть frontend сервер

```powershell
npm run dev
```

✅ **Очікуваний результат:**
```
VITE v6.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

Браузер автоматично відкриється на `http://localhost:5173`

---

## 🎉 Готово!

Тепер обидва сервери працюють:
- **Backend API:** http://localhost:3001
- **Frontend:** http://localhost:5173

---

## 🔐 Вхід в систему

На екрані логіну ви побачите 4 кнопки швидкого входу:

| Роль | Email | Пароль | Права |
|------|-------|--------|-------|
| **Адміністратор** | admin@pizza.com | password123 | Повний доступ |
| **Касир** | cashier@pizza.com | password123 | Створення замовлень |
| **Кухня** | kitchen@pizza.com | password123 | Управління замовленнями (кухня) |
| **Бар** | bar@pizza.com | password123 | Управління замовленнями (бар) |

### Швидкий вхід:
Просто натисніть на одну з кнопок ролей на екрані логіну!

---

## 🔧 Що робить кожна роль?

### 👨‍💼 Адміністратор
- Перегляд статистики та дашборду
- Управління продуктами (додавання, редагування, видалення)
- Управління столами
- Перегляд всіх замовлень
- Повний доступ до системи

### 💰 Касир
- Створення нових замовлень
- Вибір столу
- Додавання продуктів до замовлення
- Відправка замовлення на кухню/бар

### 👨‍🍳 Кухня
- Перегляд замовлень для кухні
- Зміна статусу замовлень:
  - Прийнято
  - Готується
  - Готово
  - Подано

### 🍸 Бар
- Перегляд замовлень для бару
- Зміна статусу замовлень
- Отримання сповіщень від кухні

---

## 🔄 Real-time оновлення

Система підтримує WebSocket для оновлень в реальному часі:

✅ **Нові замовлення** - відразу з'являються на екранах кухні/бару
✅ **Зміна статусу** - синхронізується між всіма користувачами  
✅ **Сповіщення** - голосові повідомлення коли замовлення готове
✅ **Статус столів** - оновлюється автоматично

---

## 🐛 Вирішення проблем

### ❌ Backend не запускається

**Проблема:** MySQL connection error

**Рішення:**
1. Перевірте `.env` файл в папці backend
2. Перевірте MySQL пароль
3. Перевірте чи MySQL запущений:
   ```powershell
   net start | findstr MySQL
   ```
4. Дивіться `backend/MYSQL_FIX.md` для детальних інструкцій

---

### ❌ Frontend показує помилки API

**Проблема:** Cannot connect to backend

**Рішення:**
1. Переконайтеся, що backend запущений (порт 3001)
2. Перевірте консоль backend на помилки
3. Спробуйте:
   ```powershell
   curl http://localhost:3001/health
   ```

---

### ❌ "Port already in use"

**Для backend (3001):**
```powershell
netstat -ano | findstr :3001
taskkill /PID <номер> /F
```

**Для frontend (5173):**
```powershell
netstat -ano | findstr :5173
taskkill /PID <номер> /F
```

---

## 📁 Структура проекту

```
D:\Work\Pizza\
├── backend/              ← Backend сервер (порт 3001)
│   ├── src/
│   │   ├── config/
│   │   ├── controllers/
│   │   ├── routes/
│   │   └── server.js
│   ├── .env             ← Конфігурація backend
│   └── package.json
│
└── src/                 ← Frontend (порт 5173)
    ├── components/
    ├── services/
    │   ├── api.ts       ← HTTP запити до backend
    │   └── socket.ts    ← WebSocket з'єднання
    ├── contexts/
    │   └── AuthContext.tsx
    └── App.tsx
```

---

## 🔄 Перезапуск системи

### Зупинити сервери:
В кожному терміналі натисніть `Ctrl + C`

### Запустити знову:

**Terminal 1 (Backend):**
```powershell
cd D:\Work\Pizza\backend
npm run dev
```

**Terminal 2 (Frontend):**
```powershell
cd D:\Work\Pizza
npm run dev
```

---

## 🧪 Тестування

### 1. Перевірити backend API

```powershell
# Health check
curl http://localhost:3001/health

# Login test
curl -X POST http://localhost:3001/api/auth/login `
  -H "Content-Type: application/json" `
  -d '{"email":"admin@pizza.com","password":"password123"}'
```

### 2. Перевірити frontend

Відкрийте браузер: http://localhost:5173

---

## 💡 Корисні команди

### Backend:
```powershell
cd backend
npm run dev          # Запуск з nodemon
npm start            # Запуск production
npm run test:db      # Тест з'єднання БД
npm run init:users   # Створити користувачів
```

### Frontend:
```powershell
npm run dev          # Запуск розробка
npm run build        # Збірка production
```

---

## 📞 Потрібна допомога?

### Документація:
- **Backend:** `backend/README.md`
- **MySQL проблеми:** `backend/MYSQL_FIX.md`
- **Швидкий старт:** `backend/QUICKSTART.md`

### Логи:
- Backend логи відображаються в терміналі backend
- Frontend помилки - в консолі браузера (F12)
- MySQL помилки - в логах backend

---

## ✅ Чеклист перед запуском

- [ ] MySQL запущений
- [ ] База даних `bar_kitchen_pos` створена
- [ ] Backend `.env` файл налаштований
- [ ] Користувачі створені (`npm run init:users`)
- [ ] Backend запущений (порт 3001)
- [ ] Frontend запущений (порт 5173)
- [ ] Можна увійти в систему

---

**Успішної роботи! 🎉**

*Якщо виникли проблеми - дивіться розділ "Вирішення проблем" вище*




